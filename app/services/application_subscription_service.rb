
class ApplicationSubscriptionService < SwellEcom::SubscriptionService

	#def subscribe( user, plan, args = {} )
	#	subscription = super( user, plan, args )
	#
	#	if subscription.is_a?(SwellEcom::Subscription) && subscription.persisted? && subscription.active?
	#
	#		# assign hardware
	#
	#	end
	#
	#	subscription
	#end

end
